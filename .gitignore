# Dependencies
node_modules/
.pnpm-store/

# Build artifacts
dist/
build/
src-tauri/target/
src-tauri/gen/

# Tauri Cargo.lock — Tauri es app, normalmente se commitea. Lo dejamos
# fuera del ignore (no listado acá) para que git lo tracke.

# Tests / coverage
coverage/
.vitest/
playwright-report/
test-results/

# Editor / IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/

# OS
Thumbs.db
.DS_Store

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Env / secrets
.env
.env.local
.env.*.local

# Claude Code — personal overrides (no commitear)
CLAUDE.local.md
.claude/settings.local.json

# Data sensible del cliente — fixtures locales, NUNCA commitear.
# Solo se versiona logo.png como asset de la app.
public/input-*
public/output-*
public/sample-*
public/*.pdf
public/*.xls
public/*.xlsx
public/*.csv
